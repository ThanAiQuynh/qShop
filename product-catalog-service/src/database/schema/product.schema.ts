/**
 * @deprecated
 * File này chỉ dữ nguyên để không break existing imports.
 * Các schema đã được tách riêng theo từng file table-name.schema.ts
 * Hãy import từ './index' hoặc trực tiếp từ file tương ứng.
 */
export * from './index';
